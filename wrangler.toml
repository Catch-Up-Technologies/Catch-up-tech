# Configuração recomendada para Cloudflare Pages
name = "catch-up-tech"

# Diretório onde o Next.js gera os arquivos estáticos
pages_build_output_dir = "./out"

[vars]
# Variáveis de ambiente podem ser colocadas aqui

# Nota: Para sites Next.js estáticos no Cloudflare Pages,
# a maioria das configurações é feita automaticamente no dashboard.
